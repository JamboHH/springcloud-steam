spring:
  application:
    name: steam-search
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      name: steam-search
eureka:
  client:
    service-url:
      defaultZone: http://localhost:80/eureka/
server:
  port: 8500
